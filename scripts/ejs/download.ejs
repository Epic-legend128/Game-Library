<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Game Library</title>
    <link rel="stylesheet" href="/../css/common.css" type="text/css">
  </head>
  <body>
    <nav class="navigation"> 
      <button onClick="changeScreen('home')">Home</button>
      <button onClick="changeScreen('library')">Game Library</button>
      <% if (isLoggedIn) { %>
        <script>
          document.getElementsByClassName("navigation")[0].style.gridTemplateColumns = "1fr 1fr 1fr 1fr";
        </script>
        <button onClick="changeScreen('signout')">Sign out</button>
        <button onClick="changeScreen('delete')">Delete Account</button>
      <% } else { %>
        <button onClick="changeScreen('signin')">Sign in</button>
      <% } %>
    </nav>
    <main>
        <br><br>
        <h1 class="title">Download <%- title %></h1>
        <p>Download the game <a target="_blank" href=/../games/<%- file %> download>here</a></p>
    </main>
    <footer>Library of Games, source code on <a target="_blank" href="https://github.com/Epic-legend128/Game-Library">github repository</a></footer>
    <script src="/../js/script.js"></script>
  </body>
</html>